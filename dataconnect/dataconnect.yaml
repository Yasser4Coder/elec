specVersion: "v1beta"
serviceId: "elec"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "elec-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
